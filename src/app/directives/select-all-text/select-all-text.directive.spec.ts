import { SelectAllTextDirective } from './select-all-text.directive';

describe('SelectAllTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectAllTextDirective();
    expect(directive).toBeTruthy();
  });
});
